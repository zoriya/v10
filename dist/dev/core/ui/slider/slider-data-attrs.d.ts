//#region src/core/ui/slider/slider-data-attrs.d.ts
declare const SliderDataAttrs: {
  /** Present when the user is actively dragging. */
  readonly dragging: 'data-dragging';
  /** Present when the pointer is over the slider. */
  readonly pointing: 'data-pointing';
  /** Present when dragging or pointing is active. */
  readonly interactive: 'data-interactive';
  /** Current axis of slider movement (`horizontal` or `vertical`). */
  readonly orientation: 'data-orientation';
  /** Present when the slider is non-interactive. */
  readonly disabled: 'data-disabled';
};
//#endregion
export { SliderDataAttrs };
//# sourceMappingURL=slider-data-attrs.d.ts.map